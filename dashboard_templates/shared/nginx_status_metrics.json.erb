{
  "graphs": [
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:cf.collector.MetronAgent.forwarder.cloudops_tools.nginx_status.nginx_waiting{*} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Core: nginx_status nginx waiting thread"
   },

   {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:cf.collector.MetronAgent.forwarder.cloudops_tools.nginx_status.nginx_active{*} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Core: nginx_status nginx active thread"
   },
   {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:cf.collector.MetronAgent.forwarder.cloudops_tools.nginx_status.nginx_reading{*} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Core: nginx_status nginx reading thread"
   },
   {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:cf.collector.MetronAgent.forwarder.cloudops_tools.nginx_status.nginx_writing{*} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Core: nginx_status nginx writing thread"
   }
  ],
  "title": "<%= environment %> nginx_status Metrics",
  "description": "created by cfaccounts+datadog-prod@pivotallabs.com"
}

{
  "graphs": [
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:datadog.nozzle.routing_api.total_subscriptions{deployment:<%= deployment %>} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Total Subscriptions (Datadog Nozzle)"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:datadog.nozzle.routing_api.total_routes{deployment:<%= deployment %>} by {ip}",
            "type": "line"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Total Routes (Datadog Nozzle)"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:datadog.nozzle.routing_api.total_subscriptions{deployment:<%= deployment %>} by {ip}",
            "type": "area"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Total Subscriptions (Metron Agent Forwarder)"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:datadog.nozzle.routing_api.total_routes{deployment:<%= deployment %>} by {ip}",
            "type": "line"
          }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Total Routes (Metron Agent Forwarder)"
    }
  ],
  "description": "Dashboard for the Routing API",
  "title": "<%= environment %> Routing API Metrics"
}

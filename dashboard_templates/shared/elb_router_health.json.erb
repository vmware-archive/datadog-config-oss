{
  "graphs": [
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.ec2.network_in{*} by {name}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.ec2.network_in"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.ec2.network_out{*} by {name}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.ec2.network_out"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "sum:aws.elb.request_count{*} by {host}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.request_count"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.elb.httpcode_elb_5xx{host:cf-router} by {availability-zone}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.httpcode_elb_5xx"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.elb.httpcode_backend_5xx{host:cf-router} by {availability-zone}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.httpcode_backend_5xx"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.elb.latency{host:cf-router} by {availability-zone}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.latency"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:aws.elb.surge_queue_length{host:cf-router} by {availability-zone}",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.surge_queue_length"
    },
    {
      "definition": {
        "requests": [
          {
            "q": "avg:aws.elb.healthy_host_count{host:cf-router}"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          },
          {
            "q": "Finish update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.healthy_host_count"
    },
    {
      "definition": {
        "requests": [
          {
            "q": "avg:aws.elb.un_healthy_host_count{host:cf-router}"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          },
          {
            "q": "Finish update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "aws.elb.un_healthy_host_count"
    },
    {
      "definition": {
        "requests": [
          {
            "q": "per_second(sum:datadog.nozzle.gorouter.total_requests{deployment:<%= metron_agent_deployment %>} by {job,index})",
            "stacked": false
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          },
          {
            "q": "Finish update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "Requests per second, by router"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "avg:datadog.nozzle.gorouter.total_routes{deployment:<%= metron_agent_deployment %>} by {job,index}",
            "type": "line"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "Total routes by router"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "per_second(avg:datadog.nozzle.gorouter.bad_gateways{deployment:<%= metron_agent_deployment %>} by {job,index})",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "router.bad_gateways per second"
    },
    {
      "definition": {
        "viz": "heatmap",
        "requests": [
          {
           "q": "avg:datadog.nozzle.gorouter.latency{deployment:<%= metron_agent_deployment %>} by {job,index}",
            "conditional_formats": []
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "Router Latency"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "per_second(avg:datadog.nozzle.gorouter.rejected_requests{deployment:<%= metron_agent_deployment %>} by {job,index})",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "router.rejected_requests per second"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "per_second(avg:datadog.nozzle.gorouter.total_requests{deployment:<%= metron_agent_deployment %>} by {job,index})",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "router.requests per second"
    },
    {
      "definition": {
        "viz": "timeseries",
        "requests": [
          {
            "q": "per_second(avg:datadog.nozzle.gorouter.responses{deployment:<%= metron_agent_deployment %>} by {job,index})",
            "type": "area"
          }
        ],
        "events": [
          {
            "q": "Begin update deployment for '<%= deployment %>' against Director"
          }
        ]
      },
      "title": "router.responses per second"
    }
  ],
  "description": "Health statistics from the routers and their ELBs",
  "title": "<%= environment %> Router and ELB health metrics"
}

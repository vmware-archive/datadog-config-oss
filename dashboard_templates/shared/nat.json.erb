{
  "graphs": [
    {
      "definition": {
        "requests": [
            {
                "q": "aws.ec2.network_in{job:nat-box-z1}"
            },
            {
                "q": "aws.ec2.network_in{job:nat-box-z2}"
            },
            {
                "q": "aws.ec2.network_in{job:nat-box-z3}"
            }        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "Network In/Out"
    },
    {
      "definition": {
        "requests": [
          { "q": "aws.ec2.cpuutilization{job:nat-box-z1}" },
          { "q": "aws.ec2.cpuutilization{job:nat-box-z2}" },
          { "q": "aws.ec2.cpuutilization{job:nat-box-z3}" }
        ],
        "events": [
          <%= default_events_json %>
        ]
      },
      "title": "CPU Utilization"
    }
  ],
  "title": "<%= environment %> NAT"
}

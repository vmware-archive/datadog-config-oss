<% throw(:skip) if job == "hello" %>
{
    "notify_no_data": false,
    "name": "<%= environment %> alert <%= job %>",
    "silenced": false,
    "query": "some query",
    "message": "<%= job %> <%= backend_pagerduty %>"
}

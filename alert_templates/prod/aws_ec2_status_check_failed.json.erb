{
  "notify_audit": false,
  "timeout_h": 0,
  "query": "max(last_1h):max:aws.ec2.status_check_failed{*} by {deployment,index,instance_id,job} > 1",
  "message": "<%= cloudops_non_critical_pagerduty %>",
  "id": 161186,
  "name": "AWS Status Check failed for {{job.name}}/{{index.name}} on {{deployment.name}} (instance-id: {{instance_id.name}})",
  "no_data_timeframe": 16,
  "notify_no_data": false,
  "renotify_interval": null,
  "escalation_message": "",
  "silenced_timeout_ts": null
}

{
  "notify_no_data": true,
  "no_data_timeframe": 20,
  "name": "<%= environment %> Logsearch: Unhealthy below threshold for logstash",
  "silenced": false,
  "query": "avg(last_5m):min:bosh.healthmonitor.system.healthy{job:logstash} < 0.8",
  "message": "One or more of the logstash BOSH agents are reporting either that the VM that they are running on or the  logstash job that they are running is unhealthy. <%= monitoringAndMetrics_email %>"
}

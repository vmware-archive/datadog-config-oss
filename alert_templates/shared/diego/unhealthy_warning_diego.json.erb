{
    "notify_no_data": false,
              "name": "<%= environment %> Diego: BOSH Agent reporting as unhealthy",
          "silenced": false,
             "query": "avg(last_5m):avg:bosh.healthmonitor.system.healthy{deployment:<%= deployment %>-diego} by {job,index} < 1",
           "message": "One or more of the BOSH agents in Diego deployment are reporting either that the VM that they are running on or the job that they are running is unhealthy.  <%= cloudops_non_critical_pagerduty %> "
}

{
    "notify_no_data": false,
              "name": "<%= environment %> Micro: High usage of available EC2 Compute Units",
          "silenced": false,
             "query": "avg(last_30m):avg:aws.ec2.cpuutilization{name:<%= micro_deployment %>} > 80",
           "message": "Micro has been using more than 80% of its available compute units on average over the last 30 minutes. <%= bosh_pagerduty %>"
}

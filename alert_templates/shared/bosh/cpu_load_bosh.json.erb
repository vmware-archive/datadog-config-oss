{
    "notify_no_data": false,
              "name": "<%= environment %> Bosh: A job is reporting a high CPU load average",
          "silenced": false,
             "query": "avg(last_10m):avg:bosh.healthmonitor.system.load.1m{deployment:<%= bosh_deployment %>} by {job,index} > 5",
           "message": "Health monitor reports a CPU load average (1m) above 5. <%= cloudops_pagerduty %>. Escalation Path: It depends on the job."
}

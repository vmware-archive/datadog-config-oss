{
    "notify_no_data": true,
              "name": "<%= environment %> Micro: AWS status check not reporting",
          "silenced": false,
             "query": "avg(last_1h):avg:aws.ec2.status_check_failed{name:<%= micro_deployment %>} > 0",
           "message": "One or more of the configured AWS status checks have not been reporting. <%= cloudops_non_critical_pagerduty %>"
}

{
    "notify_no_data": false,
    "name": "<%= environment %> Space Mailer Timeout",
    "silenced": false,
    "query": "avg(last_5m):avg:space_mailer.timeout{*} > 1",
    "message": "The space email forwarder is averaging one delivery timeout per minute. <%= services_pagerduty %> <%= services_email %>"
}

{
  "silenced": false,
  "query": "avg(last_15m):max:aws.ebs.volume_queue_length{*} by {deployment,job,index} > 50",
  "message": "the EBS queue length has spiked above 50 for the last 15 minutes on {{deployment.name}} {{job.name}}  {{index.name}}  @pagerduty-datadog-cloudops-non-critical",
  "name": "ebs queue length too high",
  "no_data_timeframe": 30,
  "notify_no_data": true
}

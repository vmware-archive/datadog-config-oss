{
  "notify_no_data": false,
  "name": "<%= environment %> CF: Nyet tests are too slow",
  "silenced": false,
  "query": "avg(last_30m):avg:nyet.full_run.response_time_ms{deployment:<%= deployment %>,app_type:ruby} > <%= params.fetch('nyet_timeout_secs') * 1000 %>",
  "message": "The nyet tests are taking longer than <%= params.fetch('nyet_timeout_secs') %> seconds to run. <%= backend_pagerduty %>"
}

{
    "notify_no_data": false,
              "name": "<%= environment %> CF: A job is running out of system disk",
          "silenced": false,
             "query": "avg(last_10m):avg:bosh.healthmonitor.system.disk.system.percent{deployment:<%= deployment %>} by {job,index} > 92",
           "message": "Healthmonitor reports system disk is > 92% full. <%= backend_pagerduty %>"
}

{
  "notify_no_data": true,
  "name": "<%= environment %> CF: AWS has instance on eu-west-1",
  "silenced": false,
  "query": "max(last_5m):max:aws.ec2.cloudops.instance_count{region:eu-west-1} > 0",
  "message": "eu-west-1 should have no instances, any instances that exist are possibly fraudulent. <%= cloudops_email %>"
}


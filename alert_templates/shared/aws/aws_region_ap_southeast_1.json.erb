{
  "notify_no_data": true,
  "name": "<%= environment %> CF: AWS has instance on ap-southeast-1",
  "silenced": false,
  "query": "max(last_5m):max:aws.ec2.cloudops.instance_count{region:ap-southeast-1} > 0",
  "message": "ap-southeast-1 should have no instances, any instances that exist are possibly fraudulent. <%= cloudops_email %>"
}


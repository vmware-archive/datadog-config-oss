{
  "notify_no_data": false,
  "name": "<%= environment %> CF: AWS has instance on ap-southeast-2 (usally fraudulent incident)",
  "silenced": false,
  "query": "max(last_5m):max:aws.ec2.host_ok{region:ap-southeast-2} > 0",
  "message": "This alert should only be triggered when AWS has instance in ap-southeast-2 <%= backend_email %>"
}


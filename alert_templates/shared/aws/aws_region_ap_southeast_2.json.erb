{
  "notify_no_data": true,
  "no_data_timeframe": 1500,
  "name": "<%= environment %> CF: AWS has instance on ap-southeast-2",
  "silenced": false,
  "query": "max(last_5m):max:aws.ec2.cloudops.instance_count{region:ap-southeast-2} > 0",
  "message": "ap-southeast-2 should have no instances, any instances that exist are possibly fraudulent. <%= cloudops_email %>"
}

